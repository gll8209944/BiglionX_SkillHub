{
  "buildCommand": "cd apps/web && npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "NEXTAUTH_URL": "https://skillhub.proclaw.cc"
  }
}
